# SQL compatibilidad (Fase 2)

Archivos `vw_*.sql` proponen vistas de lectura para capa adaptadora sin romper contratos actuales.
Aplicar primero en staging replica y validar paridad de payloads antes de promover.
